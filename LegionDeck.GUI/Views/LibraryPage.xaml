<Page
    x:Class="LegionDeck.GUI.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:LegionDeck.GUI.Models"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Page.Resources>
        <models:SourceToColorConverter x:Key="SourceToColorConverter" />
    </Page.Resources>

    <Grid RowDefinitions="Auto,*" XYFocusKeyboardNavigation="Enabled">
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Refresh" Label="Scan Library" Click="RefreshButton_Click" />
            <AppBarSeparator />
            <AppBarElementContainer>
                <ComboBox x:Name="ViewModeCombo" SelectionChanged="ViewMode_SelectionChanged" Width="150" VerticalAlignment="Center" Margin="0,0,8,0">
                    <ComboBoxItem Content="Installed" Tag="Installed" IsSelected="True" />
                    <ComboBoxItem Content="Steam Library" Tag="Steam" />
                    <ComboBoxItem Content="Xbox / Game Pass" Tag="Xbox" />
                    <ComboBoxItem Content="Ubisoft+ Library" Tag="Ubisoft" />
                    <ComboBoxItem Content="EA Play Library" Tag="EA" />
                </ComboBox>
            </AppBarElementContainer>
            <AppBarSeparator />
            <AppBarButton Icon="Sort" Label="Sort">
                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Name (A-Z)" Click="SortByName_Click" Tag="NameAsc" />
                        <MenuFlyoutItem Text="Name (Z-A)" Click="SortByName_Click" Tag="NameDesc" />
                        <MenuFlyoutItem Text="Source" Click="SortBySource_Click" />
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <CommandBar.Content>
                <AutoSuggestBox x:Name="SearchBox" 
                                QueryIcon="Find" 
                                PlaceholderText="Search library..." 
                                Width="300" 
                                Margin="12,0" 
                                VerticalAlignment="Center"
                                QuerySubmitted="SearchBox_QuerySubmitted"
                                TextChanged="SearchBox_TextChanged"/>
            </CommandBar.Content>
        </CommandBar>

        <GridView Grid.Row="1"
                  x:Name="LibraryGridView"
                  Margin="12"
                  Padding="0,0,0,24"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="LibraryGridView_ItemClick"
                  XYFocusKeyboardNavigation="Enabled"
                  XYFocusUpNavigationStrategy="NavigationDirectionDistance"
                  XYFocusDownNavigationStrategy="NavigationDirectionDistance"
                  XYFocusLeftNavigationStrategy="NavigationDirectionDistance"
                  XYFocusRightNavigationStrategy="NavigationDirectionDistance"
                  IsTabStop="True"
                  UseSystemFocusVisuals="True"
                  IsFocusEngagementEnabled="True"
                  TabFocusNavigation="Cycle"
                  PreviewKeyDown="LibraryGridView_PreviewKeyDown"
                  GettingFocus="LibraryGridView_GettingFocus">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:LibraryGameViewModel">
                    <Grid Width="180" Height="380" Margin="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Hide Game" Click="HideGame_Click" Icon="HideBcc" />
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        <StackPanel Padding="0" Spacing="0">
                            <!-- 2:3 Aspect Ratio for Steam Artwork (180x270) -->
                            <Grid Height="270" Background="{ThemeResource ControlFillColorSecondaryBrush}" CornerRadius="8,8,0,0">
                                <Image Source="{x:Bind ImgCapsule, Mode=OneWay}" 
                                       Stretch="Uniform" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       ImageFailed="Image_ImageFailed" />
                            </Grid>
                            
                            <StackPanel Padding="10" Spacing="4">
                                <!-- Allow 2 lines for names -->
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontWeight="Bold" 
                                           TextWrapping="Wrap" MaxLines="2" Height="40"
                                           TextTrimming="CharacterEllipsis" FontSize="13" />
                                <TextBlock Text="READY TO PLAY" Foreground="#FF2ECC71" FontWeight="ExtraBold" FontSize="11" Margin="0,0,0,4" />
                                <Grid>
                                    <Border Background="{x:Bind Source, Converter={StaticResource SourceToColorConverter}}" HorizontalAlignment="Left" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Source}" FontSize="10" Foreground="White" FontWeight="Bold" />
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <TextBlock x:Name="NoGamesText" Grid.Row="1" Text="No games found. Check your filters or Settings." 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Opacity="0.5" Visibility="Collapsed" Style="{ThemeResource SubtitleTextBlockStyle}" />
        
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="False" Width="60" Height="60" />
    </Grid>
</Page>