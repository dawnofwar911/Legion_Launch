<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="LegionDeck.GUI.Views.WishlistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:LegionDeck.GUI.Views"
    xmlns:models="using:LegionDeck.GUI.Models"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*">
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Refresh" Label="Sync Wishlist" Click="SyncButton_Click" />
        </CommandBar>

        <GridView Grid.Row="1"
                  x:Name="WishlistGridView"
                  Margin="12"
                  Padding="0,0,0,24"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  XYFocusKeyboardNavigation="Enabled">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:SteamWishlistItemViewModel">
                    <Grid Width="180" Height="340" Margin="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <StackPanel Padding="0" Spacing="0">
                            <Grid Height="240" Background="{ThemeResource ControlFillColorSecondaryBrush}" CornerRadius="8,8,0,0">
                                <Image Source="{x:Bind ImgCapsule}" Stretch="UniformToFill" />
                            </Grid>
                            <StackPanel Padding="10" Spacing="4">
                                <TextBlock Text="{x:Bind Name}" FontWeight="Bold" MaxLines="1" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind AccessStatus}" Visibility="{x:Bind AccessVisibility}" Foreground="Green" FontWeight="Bold" FontSize="11" />
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Border Background="DarkGreen" CornerRadius="4" Padding="4,1" Visibility="{x:Bind GamePassVisibility}">
                                        <TextBlock Text="GP" FontSize="9" Foreground="White" />
                                    </Border>
                                    <Border Background="Red" CornerRadius="4" Padding="4,1" Visibility="{x:Bind EaPlayVisibility}">
                                        <TextBlock Text="EA" FontSize="9" Foreground="White" />
                                    </Border>
                                    <Border Background="Blue" CornerRadius="4" Padding="4,1" Visibility="{x:Bind UbisoftPlusVisibility}">
                                        <TextBlock Text="U+" FontSize="9" Foreground="White" />
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="False" Width="60" Height="60" />
    </Grid>
</Page>
